[user]
	name = Grzegorz Unijewski
	email = grzegorz@example.com
[core]
	editor = /usr/bin/vim
	excludesfile = ~/.gitignore_global
	hooksPath = ~/.dotfiles/git/hooks
[filter "lfs"]
	required = true
	process = git-lfs filter-process
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
